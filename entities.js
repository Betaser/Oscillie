const entities = [];

function updateEntities() {
    for (const entity of entities) {
        // entity.update();
    }
}

function updateRenderEntities() {
    for (const entity of entities) {
        // set the bounds
    }
}